class SellService
  def process_sell(user_id, product_id, ft)
    # product = get_product(product_id)
    # create_sell(product_id)
    # get_exchange_rate(product["currency"])
    # send_bonus(user_id, product["price"])
  end
end
